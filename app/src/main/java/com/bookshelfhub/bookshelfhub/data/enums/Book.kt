package com.bookshelfhub.bookshelfhub.data.enums

enum class Book(val KEY:String) {
    NAME("title"),
    ISBN("isbn"),
    IS_SEARCH_ITEM("is_search_item"),
    AUTHOR("author"),
    RATING_DIFF("rating_diff")
}